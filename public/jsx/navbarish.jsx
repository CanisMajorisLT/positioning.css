var React = require('react');

module.exports = React.createClass({
    render: function () {
        return (
            <div id="navbar"><div className="logo">CSS Positioning</div></div>
        )
    }
});